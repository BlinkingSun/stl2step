# 1.3.0 — N-gon cylinders, planar CIRCLE edges, chamfer cones

Fixture: `~/Desktop/test.stl` (Mastercam, 3222 tris). `test.step` is **engine output**, not CAD GT.

## What 1.2.0 did

TrueForm **reverted the whole component** (`smoothRevertedComponents=1`, `smoothBuiltCylinders=0`). The 1517 “planes” are Stage-1 coplanar unify, not analytic rebuild. Holes/arcs/chamfers stay faceted.

Mesh (Ẑ holes):

| Feature | Geometry |
|---|---|
| Hole A | R=12.625 mm, 56-gon, θ=6.43° |
| Hole B | R=8.500 mm, ~184-gon, θ≈1.96° |
| Chamfer on B | 45.017° to Ẑ, R 8.5→9.5 mm, cone frustum |
| Outer arcs | R=30 and R=17, regular polylines |
| Slot ends | R=3 mm semicircles |

## Why the existing path misses them

1. **Claim order:** A2 grows **planes first**. N-gon walls are not coplanar, so A3 cannot merge them into a cylinder ([agent 04](3a17d33b-d584-4b7b-af85-adcee98d2bda)).
2. **3222-tri dead zone:** outside `coarseFusionBand` (500–1200) so no chord-sagitta G2; above arch-commit (≤2500). Off-coarse G2 is 1%R; N=12 sagitta is already 3.4%R ([agent 03](35f71987-ec5a-42db-8dad-258d99d83676), [agent 07](06403a85-4ee3-4b39-8bec-23a1c7347fa2)).
3. **Seed φ ∈ [5°, 60°]:** Hole B’s 1.96° dihedral is **below** 5°. Fine tessellation is worse for B1, not better.
4. **CIRCLE edges** only from plane∩cyl. No cylinder neighbor → plane|plane IntAna is line-only → polyline ([agent 06](729d5963-fdd5-4e0a-a7b3-0ce658980e50)).
5. **Chamfer:** C1 fillet is plane–plane 1–3 rows, shipped as **cylinders**. Hole-mouth 45° is a **cone**; `SurfType::Cone` is ConeNYI; cyl neighbor → TorusNYI ([agent 05](9097b323-33de-4651-ab53-2bdb8b07e74d)).

Do **not** widen `coarseFusionBand` (Handle pickup ~908). Do **not** revive E′ axis-angle demotion (S02/S11-b).

## Detectors (insert, do not reorder L/B1/A3 on already-claimed tris)

Order: `A1 → A2 → L → A → C(pass 1) → B1 → C(pass 2) → C1 → A3` (978ab69); **B** at chain collapse.

- **A — closed N-gon cylinder** (unclaimed A2 walls, N≥6, generators ∥, axis from successive plane intersections or cap normal; accept test is the vertex residual against the fitted circle, ad5c814 — no chord-sagitta term). Origin `NgonWall`. Skips turning-axis blends and cones. Can certify a through hole (ad5c814).
- **B — CIRCLE on already-analytic planes** (LS circle on loop verts, N≥6). Same handle for neighbor cyl/cone. Stadium leftovers stay mixed. Never force-polyline on analytic|analytic. Never overrides an IntAna curve of the two shipped surfaces (D-130-2; 74f885c).
- **C — chamfer cone** (pass 1 after A, before B1; pass 2 after B1, before C1): ring adjacent to one closed360 cylinder and one cap, shared axis, two radii, constant α, `|n·axis|=sin α`. Origin `ChamferCone` → `Geom_ConicalSurface`. Other cones/tori/spheres stay NYI (U-3; D-130-3, D-130-7).

## Battery

1. Handle pickup: 916 faces / 795 PLANE / 121 CYLINDRICAL / 97 CIRCLE, |dV| 9.732831, watertight, reverted=0 (D-130-13(1) addendum). No A/C on GT blends.
2. S11-b: 12/12 fillet cylinders; **zero** new cones; valid re-read solid (8c1498d).
3. Plate `linkage_bores_chamfer`: 17 cylindrical walls + 2 ChamferCone frusta, watertight, 0 reverted; hole-B rims CIRCLE. Cross bore 214/279 in 2 faces (union deferred, D-130-17). Contract owned by `gate_130` (`tests/gates/gate_130.py`). B2–B5 green; B6 expected-red (D-130-5).

Gate files: `tests/corpus/linkage_bores_chamfer.stl` + `.expected.json`; `tests/gates/gate_130.py` (D-130-18(3) — there is no `gates/linkage_chamfer_gate.py`).

## Implementation notes

Shipped in 1.3.0 (`STL2STEP_VERSION_*` / `project(... VERSION 1.3.0)`):

- `Origin::NgonWall` / `Origin::ChamferCone` on `enum class Origin` in `src/refit.hpp`.
- Pipeline comments + declarations in `src/refit_internal.hpp`: `claimNgonWallsA`, `claimChamferConesC`, `tryPlaneLoopCircles`, plus `loopIsCircle` / `planeLoopCircleOf` / `planeLoopCircleForChain` for the collapse-time CIRCLE cache. Order is `A1 → A2 → L → A → C(pass 1) → B1 → C(pass 2) → C1 → A3`.
- `src/refit_segment.cpp` `runStages` calls A after L, C pass 1 after A (before B1), and C pass 2 after B1 (978ab69).
- `CMakeLists.txt` lists `src/refit_ngon.cpp`, `src/refit_chamfer_cone.cpp`, `src/refit_plane_circle.cpp`, `src/refit_cone_math.cpp`, `src/refit_cone_bind.cpp`, `src/stl_quant.cpp` on `stl2step_core`.
- G2 in `evaluateCommit` (`src/refit_grow.cpp`): outside coarse, `N = ev.d2.nSides` only when `N >= 6`; otherwise `epsCylAccept(R)` (75101e7; D-130-1). `coarseFusionBand` is not widened.
- `src/refit_chains.cpp` census: `NgonWall` counts as a cylinder; `ChamferCone` is neither a fillet nor a radius sample.
- `src/refit_fillet.cpp`: `ChamferCone` is not a C1 neighbour; `NgonWall` is a committed cylinder like `CylGrow`.
- Fixture: `tests/corpus/linkage_bores_chamfer.stl` + `.expected.json`. Contract: `tests/gates/gate_130.py`.

## Handoff (2026-09-05, release) — the shipped 1.3.0 state

Measured at tip `9fa4eff` (BIND + CYLCYL merged; version bump is this lane) on
`tests/corpus/linkage_bores_chamfer.stl`, `--smooth`, verified path. 1.2.0 on the
same file: `smoothRevertedComponents=1`, 1517 planar facets, nothing analytic.

### What builds on the plate

| feature | ground truth (sidecar) | shipped at 1.3.0 | landed by |
|---|---|---|---|
| hole A, R 12.625, closed 360 × 2 | 112 + 112 tris | **2 closed cylinder faces**, whole; rims CIRCLE | law band L (1.2.0-era `cylGrow`), caps valid since 714a6af / 7a1c822 |
| hole B, R 8.5, closed 360 × 2 | 139 + 139 | **2 closed cylinder faces**, 139/139 each; rims CIRCLE | absorb (4af42c2, D-130-12) then virtual-generator chain (e2cf8f5, D-130-8) |
| chamfer frustum R 8.5 → 9.5, 45.000004° × 2 | 184 + 184 | **2 `Geom_ConicalSurface` faces**, `valid=1`, both taper signs; rims shared as tier-1 circles | b50328c, 30f67dd, 10ce7cd (D-130-3) |
| outer profile arc R 30 × 2 | 75 + 80 | **2 partial cylinder faces**, exactly 75 and 80 | absorb (D-130-12) |
| outer profile arc R 17 × 2 | 68 + 66 | **2 partial faces**, whole | L (1.2.0-era) |
| waist arcs R 5 × 2 each | 12 + 12, 6 + 6 | **4 partial faces**, whole | L |
| slot ends R 3 × 4 walls | 39 per wall | **8 partial faces** (2 per wall, 28 of 39 tris each); the ragged top row stays facets — the mesh has 36 interior edges for 39 triangles (130-strips §2.3) | e2cf8f5 (D-130-8) |
| cross bore R 10, +X, 356.4° | 279 (255 on-surface + 24 off by 7.08e-4 = 54 q; D-130-13(2), BIND re-measure) | **2 partial faces, 214 of 279** (seed pieces 108 + 106); junction triangles and the pieces they cut off stay facets | absorb + virtual generators; extent by D-130-13(2) / D-130-17 |
| toroidal rounds at the cross-bore mouths | 568 tris, 3 bands | **facets** | deferred, D-130-7 |

The plate sidecar's cross-bore entry is the mesh truth (255 + 24) and is **not**
edited to match the shipped 214/279; the plate's `gates_full` SIDECAR row is
**expected-red** by D-130-17 (addendum 2026-09-05). I7 on the plate is green
(b53029b, e523298; D-130-21). Corpus controls: Handle pickup 916 / 795 / 121 / 97,
|dV| 9.732831; `S11-b` 12/12 and a valid re-read solid (8c1498d); `S12-b` refused;
Body28 builds (D-130-6). RESULT carries `smoothBuiltCones` (plate = 2) and a
sorted `warnings` list (b94a094; D-130-10). The edge-class census is on the
shipped shell (9249ab0, 082194b; D-130-2); nine fixtures are recorded expected-red.

B2 `cross_bores` builds: one R8 closed-360 face with two tier-2 inner wires, two
R5 faces, watertight, reverted 0 (b8b3291; D-130-16/2). Volume cell green at
0.00272 % vs the generator's exact volume after adaptive GProp (9fa4eff;
D-130-20) — the residual 1.70 mm³ is the tier-2 chord, counted not absorbed.
B3/B4/B5 cones 1/1, cylinders per sidecar. B6 `cyl_meets_chamfer` stays
expected-red (D-130-5).

### What is deferred, and why

- **Torus surfaces (D-130-7 → 1.4).** The plate's three toroidal blends at the
  cross-bore mouths are real rounds, not cylinders; no lane builds
  `Geom_ToroidalSurface`. The 13 two-facet R≈2 fillet-strip slivers on those
  bands (D-130-9(a), D-130-10(3)) are the count 1.4 inherits.
- **Same-surface islands and the loop-level union (D-130-13(2), D-130-14,
  D-130-16, D-130-17 → 1.4).** The islands landed and were reverted (228dfbc →
  000c29f: one wall as eleven faces, refused by `partial_recovery_gate`). The
  union is compiled-in behind `STL2STEP_UNION`, off by default (d3179af, 0477c72).
  Every face the union fails is a pinched domain — pieces of one surface that
  share only a mesh vertex — and the pinches are the tessellator's blend
  triangles against the unbuilt tori. Shipped: seed pieces (cross bore 214/279
  in 2 faces; slot-end top rows faceted). Plate SIDECAR expected-red (D-130-17
  addendum).
- **B6 `cyl_meets_chamfer`** (non-coaxial cone∩cyl, trimmed cone face) stays
  expected-red and may slip to 1.3.1 (D-130-5).
- **Edge-class red lines on 9 fixtures** (082194b; D-130-2), recorded in
  `edge-class-ratchet.json` `expectedRed`. Not relaxed.
- General cones (apex in the face, oblique sections) are 1.3.1+ (D-130-3).
  Free-BSpline and `BRepAlgoAPI_Section` intersection curves are refused
  (D-130-2, door D-130-2b).
- Hand constants named and not moved: `thetaCylLoDeg = 5.0`, `kTurnDot = 0.7`
  (D-130-8). The tripwire of D-130-5 reports and does not refuse until its
  resolver is pass-aware (8c1498d).

### The decisions that changed the design

**D-130-2 — intersection edges by tier.** An edge ships analytic only where
`IntAna` on the two *shipped* surfaces returns a line, circle or ellipse;
otherwise it ships as the mesh polyline, counted and never absorbed; free
BSplines and Section curves are refused. First consequence: detector B had been
replacing exact plane|plane lines with fitted arcs (74f885c). The edge-class
census (9249ab0, 082194b) proves the invariant; nine fixtures are recorded
expected-red. B2's cyl∩cyl windows are tier 2, shared when both faces reference
one EDGE_CURVE (8dd2503; D-130-19).

**D-130-8 — no angular floor decides curvature.** `thetaCylLoDeg = 5.0` gated
B1 while hole B folds at 1.96°, the cross bore at 1.8° and the slot ends at
3.78°. The ruling replaced it with a geometric predicate certified by the vertex
residual; the generator is the facet-plane intersection line where the mesh
drew no edge (virtual generators, e2cf8f5). The floor still exists in the code.

**D-130-9 — the absorb lands; strips yield to a recognised cylinder; one facet
may not explode a component.** (a) Strips adjacent to the newly recognised R 10
bore lose fillet status — amending D-130-7(b): a two-facet sliver on a torus is
a mis-typed surface. (b) One unorientable *facet* fill face took the component
to the U2 blanket rung — the facet is filled on its own wire's plane (0f065e7)
and a non-region face explodes only its neighbourhood (66608dc).

**D-130-12 — the certificate is the mesh's quantization.** D-130-11(1)'s planar
noise floor is A2's plane tolerance showing through (max 0.019 mm vs median
1.7e-15). The ruling names the binary32 ulp at the mesh's own coordinate
magnitude (`q`, 61fab1d) and requires a free-axis least-squares refit after
every absorb round (4af42c2).

**D-130-13 — the Handle pickup heal is recorded; the cross bore's islands.**
(1) Two R≈3 bands admit three triangles by `max(bandResid, q)`; the control is
re-baselined (916 / 795 / 121 / 97). (2) The cross bore's 255 on-surface
triangles are cut into six pieces by 24 junction triangles at 7.08e-4 mm; the
sidecar records that mesh truth.

**D-130-14 / D-130-16 / D-130-17 — one surface, one face; then the union is
deferred.** Pieces of one certified surface are unified at the loop level, but
every failing union face is a pinched domain (pieces meeting at a vertex), which
no OCCT face carries, and the pinches are the blend triangles against rounds
1.3.0 does not build. 1.3.0 ships the seed pieces; the union lands with the tori
in 1.4. D-130-16's two-arc rim collapse (75d043b) is live on B2's closed-360 R8
face with two inner wires (b8b3291).

**D-130-15 / D-130-19 / D-130-20 — `gate_130` follows the geometry.** The volume
cell compares against the sidecar's `exactVolume` from the generator (9ccaf8d).
The `coneNYI` premise is retired (6952dcc). Sharing is one EDGE_CURVE on both
analytic faces' loops (8dd2503). The cyl cell counts distinct surfaces, not
faces (53c46f6). B2's 37 mm³ was default GProp under-integrating 68-span
polyline windows, not a short bore (9fa4eff); residual 1.70 mm³ is the tier-2
chord.

**D-130-18 — instruments match their own definitions.** Plate sidecar `nSides`
is the checker's D2 measurement (2d7cb78). The compose gate's S06 origin cell
is the NgonWall claim (bb89a49). CHANGELOG names `gate_130`, not a
non-existent `linkage_chamfer_gate.py` (8a646f5).

**D-130-21 — I7 heal lifts PARKED where I1–I9 closed.** A region whose only
neighbour is an island closes its own loop (b53029b); a junction vertex ends
the closing chain so I8 holds (e523298). PARKED lifted on Body11, Body28
(538/775), handle-pickup (171) (038aafb).

### What the 2026-09-03 Handoff got wrong

Its root cause — "detector B replaces plane-loop polylines with CIRCLE edges
outside the chain machinery (`ci=-1`, `makeFaceKeep-seam`)" — was an instrument
artefact: three diagnostic short-circuits printed `ci=-1` before the chain index
was computed (7a1c822). The caps failed for two other reasons, both fixed: a
doubled reversal on single-closed-edge hole loops (714a6af) and the sew-budget
cap on a plate whose mesh really sits 0.0037 mm off its plane (7a1c822). The 30
`MakeEdge failed` warnings were vertices moved by the plate `ShapeFix_Face` rung
(e7c00a8).

## Cone math (lane 130-CONE-MATH, SPEC-130-cone-math)

Published signatures — `src/refit_cone_math.hpp`, implemented in
`src/refit_cone_math.cpp`, certified by `cone_math_unit`
(`tests/unit/cone_math_test.cpp`). Pure `gp_` geometry: no `MeshView`, no
`Region`, no build-path change. `refit_internal.hpp` does not move.

The cone math is its own translation unit rather than part of `src/refit_math.cpp`
because that file is one of the five P1 sources the **D5.3 include allowlist**
covers, and the allowlist — a gate instrument (`include-allowlist` in
`tests/gates/run_gates.py`), not a style rule — admits no bare `gp.hxx` and no
project header beyond `refit.hpp` / `refit_internal.hpp`. The cone math needs
both, so it sits outside the allowlist by construction: the instrument is
unchanged and P1 keeps exactly the include surface it was measured with. The
separate header additionally lets the unit test compile the one TU on its own,
without linking the engine.

Frame, OCCT `Geom_ConicalSurface` / `gp_Cone` (the untrimmed surface is the
complete **double** cone, infinite in `v` both ways):

```
S(u,v) = O + (R + v*sin(Ang)) * (cos(u)*XDir + sin(u)*YDir) + v*cos(Ang)*ZDir
z = v*cos(Ang)          rho_cone(z) = R + z*tan(Ang)   (SIGNED; < 0 past the apex)
z_apex = -R/tan(Ang)    v_apex      = -R/sin(Ang)      v = z/cos(Ang)
```

`Ang = SemiAngle()` is non-zero in `]-PI/2, PI/2[` and may be negative (apex on
the positive side of `ZDir`); every formula below is stated for signed `Ang`.

**Classes** (`enum class ConeDevClass`, `coneDevClassName`, `coneDevClassIsExact`):

| class | when | value |
|---|---|---|
| `CoaxialRim` — `"circle-on-cone"` | circle plane ⟂ axis, centre **on** axis | **exact**, constant in `u` |
| `PerpPlane` — `"circle-on-cone-perp"` | circle plane ⟂ axis, centre **off** axis | **exact** supremum |
| `GeneralBound` — `"circle-on-cone-bound"` | tilted and/or off-axis | **provable upper bound** |
| `Unhandled` — `"unhandled-circle-on-cone"` | degenerate input | `-1.0` |

`"circle-on-cone"` is the class D-130-3 gates cone faces on. A `GeneralBound` is
never presented as the deviation: it is `>=` the true supremum, so a bind site may
compare it against a tolerance, and a comparison that fails means tier 2 (mesh
polyline, counted per D-130-2), not a widened tolerance.

**Exact point-to-surface distance.** The meridian plane through `p` meets the
double cone in two full lines, both entirely on the surface, so the perpendicular
foot is always a real surface point — no apex clamping, no ray case:

```
pointConeDist(cone, p) = cos(Ang) * | rho_p - |rho_cone(z_p)| |
```

**The supremum.** With `xi = |rho_cone(z)|` and the circle's radial span
`[rho_lo, rho_hi]`, `sup = cos(Ang) * max(rho_hi - xi, xi - rho_lo)`. For a
circle in a plane ⟂ the axis the span is exactly `[|d0 - r|, d0 + r]` (`d0` =
centre-to-axis distance), which is why (a) is exact and collapses on the axis to
`cos(Ang) * | r - |rho_cone(z)| |`. For (b) the same exact formula is evaluated
over an interval box — `z` in `z0 +- r*sin(beta)` (tight; `beta` = angle between
circle normal and cone axis) and `rho` in
`[max(0, r*cos(beta) - d0, d0 - r), d0 + r]` (the circle projects onto the plane
⟂ the axis as an ellipse of semi-axes `r` and `r*cos(beta)`, so the projected
offset has modulus in `[r*cos(beta), r]` and the triangle inequality gives both
ends) — whose maximum is taken at a corner. One evaluator serves both regimes:
at `sin(beta) = 0`, `cos(beta) = 1` the box collapses onto the exact span and the
inequality becomes an equality. The bound therefore stays tight where it matters
— a rim tilted 0.05° off a 45° cone of radius 11 bounds at 6.7907e-3 mm against a
true supremum of 6.7907e-3 mm, not O(r).

**The class is invariant under rigid placement.** A circle whose normal *is* the
cone's axis direction classes `CoaxialRim`/`PerpPlane` for every orientation of
that cone in world space. That is not automatic: two unit `gp_Dir`s that are
bitwise (anti)parallel still dot to `1 -+ 1ulp`, and `sqrt(1 - dot*dot)` turns
that ulp into a spurious tilt of ~1e-8 rad — eight orders above
`Precision::Angular()`. `sin(beta)` therefore comes from the **cross product**,
which is exactly zero for identical coordinates and carries absolute rather than
square-rooted error near parallel. Measured on 2000 random placements of the B1
cone, the `sqrt` form demoted 669 exact rims to `GeneralBound` and reported
deviations up to 1.55e-7 mm — above `Precision::Confusion()`, so it would not
have been absorbed at the bind site either. `cone_math_unit` sweeps 400
placements and asserts 0 demotions; it is red against the `sqrt` form.

The five scalar helpers (`coneRadiusAt`, `coneApexZ`, `coneApexV`, `coneVAtZ`,
`coneVAtRadius`) return **NaN**, never a plausible 0 or inf, for a cone that
could not have come from a legal `gp_Cone`.

```cpp
enum class ConeDevClass : std::uint8_t { Unhandled = 0, CoaxialRim, PerpPlane, GeneralBound };
const char* coneDevClassName(ConeDevClass c);
bool        coneDevClassIsExact(ConeDevClass c);

double coneAxialCoord (const gp_Cone& cone, const gp_Pnt& p);   // z
double coneRadialCoord(const gp_Cone& cone, const gp_Pnt& p);   // rho >= 0

double coneRadiusAt (const gp_Cone& cone, double z);            // SIGNED R + z*tan(Ang)
double coneApexZ    (const gp_Cone& cone);
double coneApexV    (const gp_Cone& cone);
double coneVAtZ     (const gp_Cone& cone, double z);
double coneVAtRadius(const gp_Cone& cone, double rho);

bool coneFrustumVRange         (const gp_Cone& cone, double zA,   double zB,
                                double& vLo, double& vHi);
bool coneFrustumVRangeFromRadii(const gp_Cone& cone, double rhoA, double rhoB,
                                double& vLo, double& vHi);
bool coneFromRims(const gp_Ax3& rimA, double rhoA, double rhoB, double height, gp_Cone& out);

double pointConeDist(const gp_Cone& cone, const gp_Pnt& p);

double circleOnConeMax(const gp_Cone& cone, const gp_Circ& circ,
                       ConeDevClass* clsOut = nullptr,
                       double angTol = Precision::Angular(),
                       double linTol = Precision::Confusion());

bool coneIntPlaneCircle(const gp_Cone& cone, const gp_Pln&      pln, gp_Circ& out,
                        double angTol = Precision::Angular(),
                        double linTol = Precision::Confusion());
bool coneIntCylCircle  (const gp_Cone& cone, const gp_Cylinder& cyl, gp_Circ& out,
                        double angTol = Precision::Angular(),
                        double linTol = Precision::Confusion());
bool coneIntConeCircle (const gp_Cone& a,    const gp_Cone&     b,   gp_Circ& out,
                        double angTol = Precision::Angular(),
                        double linTol = Precision::Confusion());
```

**Apex exclusion.** `coneFrustumVRange` refuses unless the signed cone radius is
strictly positive at both rims. That single condition *is* apex exclusion:
`rho_cone(z) = tan(Ang) * (z - z_apex)`, so two positive radii put both rims on
the same side of the apex and `[vLo, vHi]` cannot contain `v_apex`. CONE-BUILD
trims to that range and never has to special-case the apex.

**Tier-1 refusals.** `coneIntCylCircle` returns only the near-nappe root
`z = (R_cyl - R)/tan(Ang)`; the far-nappe root `z = (-R_cyl - R)/tan(Ang)` lies
below the apex, hence outside any frustum's `v`-range. `coneIntConeCircle`
solves the branch `rho_a(z) == rho_b(z)` with both **signed** radii positive; it
refuses parallel generators, and refuses the two other branches (both signed
radii equal and negative — a real circle, but on the far nappe of both cones —
and `rho_a == -rho_b`, one nappe each), neither of which can lie in the v-range
of a frustum. Non-coaxial and oblique configurations return `false` for all
three — tier 2 by D-130-2, never an approximated conic.

All three agree with `IntAna_QuadQuadGeo` on the battery numbers — the same OCCT
code `pickIntAna` runs at the bind site — and `pointConeDist` agrees with
`GeomAPI_ProjectPointOnSurf` to 1.7e-14 mm over 1105 probes, including points far
past the apex. `cone_math_unit`: **274/274 PASS in 0.19 s**.

**What CONE-BUILD still has to add.** `circleOnConeMax` is the surface-distance
supremum only, by design — it takes `gp_` types and no `Geom_`/pcurve handles, so
it stays pure. `circleOnCylMax` (`src/refit_build.cpp:6814`) additionally maxes in
`curveSurfDevAtBind` at the two bind parameters; the cone branch of
`exactMaxAtBind` must do the same, i.e.
`max(curveSurfDevAtBind(c3, f, ...), curveSurfDevAtBind(c3, l, ...), circleOnConeMax(...))`,
and map the class with `coneDevClassName`. Omitting the pcurve term would record
a tolerance that ignores the parametrisation, which is a different supremum from
the one this lane certifies.
